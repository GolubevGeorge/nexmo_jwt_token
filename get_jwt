// build command: npm install crypto-js

const sha1 = require("crypto-js/sha1");

module.exports.Get_JWT = function(secret) {
    if (secret == "x") {
        return x + " !!!!";
    } else {
        return "Error";
    }
}
