module.exports = (data) => {
    
    return new Promise((resolve, reject) => {
    data.hello = "Hello World!" + data.in;
          
        if (response) {
        data.answer = response;
        resolve(data);
      }
};
